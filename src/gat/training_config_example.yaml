data:
  num_classes:
focal_loss:
  alpha:
  enabled:
  gamma:
logging:
  checkpoint_interval:
  enable_checkpoint_saving:
  enable_final_visualization:
  enable_tensorboard:
  log_interval:
  max_visualize_districts:
model:
  add_self_loops:
  dropout:
  hidden_dim:
  negative_slope:
  num_heads:
  num_layers:
seed:
spectral_clustering:
  distance_scale:
  distance_weight:
  embedding_weight:
  feature_weight:
  max_hops:
  min_cluster_size:
  min_component_size:
  oversample_factor:
  use_confidence_weighted_voting:
training:
  batch_size:
  class_weight_smoothing:
  cv_mode:
  epochs:
  gradient_accumulation_steps:
  k_fold:
  lambda_smooth:
  lr:
  min_delta:
  node_threshold:
  num_neighbors:
  num_workers:
  patience:
  smooth_temperature:
  use_amp:
  val_interval:
  weight_decay: